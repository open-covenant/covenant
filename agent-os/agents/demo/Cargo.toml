[package]
name = "demo-agent"
version = "0.1.0"
description = "Demo agent for the public sandbox. Calls Anthropic Haiku via covenant-llm and returns a real reply, with a daily request quota."
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "demo"
path = "src/main.rs"

[dependencies]
covenant-llm = { path = "../../crates/covenant-llm" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
