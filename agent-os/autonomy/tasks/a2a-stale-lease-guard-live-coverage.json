{
  "id": "a2a-stale-lease-guard-live-coverage",
  "title": "Cover A2A stale-lease repair guard",
  "state": "integrated",
  "priority": "high",
  "ownerRole": "verifier",
  "scope": [
    "agent-os/crates/covenantd/tests",
    "agent-os/autonomy/live-coverage.json",
    "docs",
    "landing/app/docs"
  ],
  "summary": "Cover the A2A repair lease guard through real CLI subprocesses so stale or mismatched lease ids cannot requeue the wrong in-flight task.",
  "expectedFailureModes": [
    "The stale-lease guard is tested only through in-process daemon helpers, not the public CLI path.",
    "CLI argument parsing sends the wrong lease id or omits the guard and requeues unsafe work.",
    "A failed guard does not produce an operator-visible rejection that automation can classify."
  ],
  "gates": [
    "test_expansion",
    "docs"
  ],
  "verification": [
    "cd agent-os && cargo build -p covenant --locked",
    "cd agent-os && cargo test -p covenantd --test live_cli_a2a_repair --locked -- --ignored live_",
    "node agent-os/scripts/validate-live-coverage.mjs",
    "node agent-os/scripts/validate-autonomy.mjs",
    "pnpm --dir landing build",
    "bash agent-os/scripts/validate.sh --quick",
    "git diff --check"
  ],
  "humanEscalation": [],
  "nextAction": "Extend the live A2A repair fixture with a stale lease rejection before any successful requeue path.",
  "updated": "2026-05-09"
}
