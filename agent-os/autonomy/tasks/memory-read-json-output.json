{
  "id": "memory-read-json-output",
  "title": "Add JSON output for memory reads",
  "state": "integrated",
  "priority": "high",
  "ownerRole": "implementer",
  "scope": [
    "agent-os/crates/covenant",
    "agent-os/crates/covenantd/tests",
    "agent-os/autonomy/live-coverage.json",
    "docs",
    "landing/app/docs"
  ],
  "summary": "Add stable machine-readable output for `covenant memory recent --json` and `covenant memory search --json` so agents can inspect memory without parsing display lines.",
  "expectedFailureModes": [
    "Automation parses `[timestamp] tier: text` display rows and breaks when formatting changes.",
    "`--json` emits multiple records instead of one envelope with the applied filters.",
    "Search JSON omits the query or effective tier/limit, making memory-read evidence hard to replay."
  ],
  "gates": [
    "test_expansion",
    "docs"
  ],
  "verification": [
    "cd agent-os && cargo test -p covenant --locked memory_read_json",
    "cd agent-os && cargo build -p covenant --locked",
    "cd agent-os && cargo test -p covenantd --test live_cli_memory_read_json --locked -- --ignored live_",
    "pnpm --dir landing build",
    "node agent-os/scripts/validate-live-coverage.mjs",
    "node agent-os/scripts/validate-autonomy.mjs",
    "bash agent-os/scripts/validate.sh --quick",
    "git diff --check"
  ],
  "humanEscalation": [],
  "nextAction": "Continue with the next autonomous backlog item.",
  "updated": "2026-05-09"
}
