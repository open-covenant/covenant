{
  "id": "peer-scope-dispatch",
  "title": "Enforce peer capability scope predicates",
  "state": "integrated",
  "priority": "high",
  "ownerRole": "implementer",
  "scope": [
    "agent-os/crates/covenant-permissions",
    "agent-os/crates/covenantd",
    "docs",
    "landing/app/docs"
  ],
  "summary": "Define and enforce stable peer scope predicates so delegated `peers.list`, `peers.revoke`, and `peers.purge` paths cannot act outside signed scope metadata.",
  "expectedFailureModes": [
    "A scoped `peers.revoke` grant can revoke a different peer than the signed selector.",
    "A scoped `peers.list` grant can enumerate peers outside the signed scope, leaking metadata.",
    "Docs imply scoped peer permissions are enforced when dispatch still treats peer scope fields as compatibility metadata."
  ],
  "gates": [
    "plan",
    "docs",
    "security",
    "test_expansion"
  ],
  "verification": [
    "cargo test -p covenant-permissions --locked peer_scope",
    "cargo test -p covenantd --locked scope_",
    "cargo clippy -p covenant-permissions -p covenantd --all-targets --locked -- -D warnings",
    "pnpm --dir landing build",
    "node agent-os/scripts/validate-autonomy.mjs",
    "bash agent-os/scripts/validate.sh --quick",
    "git diff --check"
  ],
  "humanEscalation": [],
  "nextAction": "Continue with settlement scope predicate enforcement.",
  "updated": "2026-05-09"
}
