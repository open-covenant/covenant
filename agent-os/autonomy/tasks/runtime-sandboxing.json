{
  "id": "runtime-sandboxing",
  "title": "Introduce a real runtime sandbox boundary",
  "state": "blocked",
  "priority": "critical",
  "ownerRole": "planner",
  "scope": [
    "agent-os/crates/covenant-runtime",
    "agent-os/crates/covenantd",
    "agent-os/crates/covenant-manifest",
    "docs/runtime-sandbox-security.md",
    "docs/decisions"
  ],
  "summary": "Design and implement the first production-grade isolation backend for untrusted local agents, starting with Linux gVisor and preserving macOS subprocess mode as explicitly trusted-local only.",
  "expectedFailureModes": [
    "The project claims sandbox-grade isolation while macOS and fallback subprocess mode still run trusted-local processes.",
    "A sandbox-required agent silently falls back to subprocess execution when the configured Linux backend is unavailable.",
    "The first sandbox backend leaks host filesystem, network, or environment access through permissive defaults.",
    "The backend choice optimizes developer convenience but cannot support hostile third-party agent execution."
  ],
  "gates": [
    "plan",
    "security",
    "test_expansion",
    "docs",
    "escalation"
  ],
  "verification": [
    "bash agent-os/scripts/validate.sh --quick",
    "cd agent-os && cargo test -p covenant-runtime",
    "node agent-os/scripts/validate-autonomy.mjs"
  ],
  "humanEscalation": [
    "Hardware and OS target selection for sandbox validation",
    "Approval before claiming sandbox-grade isolation publicly"
  ],
  "nextAction": "Wire daemon backend config to select trusted-local vs linux-gvisor once the existing unrelated daemon/protocol worktree changes are clear enough to edit safely, then add live runsc coverage.",
  "updated": "2026-05-09"
}
