{
  "id": "tools-list-json-output",
  "title": "Add JSON output for tools list",
  "state": "integrated",
  "priority": "high",
  "ownerRole": "implementer",
  "scope": [
    "agent-os/crates/covenant",
    "agent-os/crates/covenantd/tests",
    "docs",
    "landing/app/docs"
  ],
  "summary": "Add stable machine-readable output for `covenant tools list --json` so agents can enumerate tool cards without parsing human text.",
  "expectedFailureModes": [
    "Automation depends on human `name — description` rendering and breaks when punctuation changes.",
    "`--json` emits multiple objects or mixes human stdout with JSON output.",
    "The machine-readable tool list diverges from the daemon registry because the CLI applies extra filtering or formatting."
  ],
  "gates": [
    "test_expansion",
    "docs"
  ],
  "verification": [
    "cd agent-os && cargo test -p covenant --locked tool_list_json",
    "cd agent-os && cargo build -p covenant --locked",
    "cd agent-os && cargo test -p covenantd --test live_cli_tools_list_json --locked -- --ignored live_",
    "pnpm --dir landing build",
    "node agent-os/scripts/validate-autonomy.mjs",
    "bash agent-os/scripts/validate.sh --quick",
    "git diff --check"
  ],
  "humanEscalation": [],
  "nextAction": "Continue with the next autonomous backlog item.",
  "updated": "2026-05-09"
}
