[package]
name = "covenant-bond"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Bonded operators with liveness slashing. Records what an operator was observed to do, assesses it against an SLA, and derives a slash from that record rather than from a caller-supplied reason."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sha2 = "0.10"
hex = "0.4"
