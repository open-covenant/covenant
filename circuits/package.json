{
  "name": "@covenant/circuits",
  "version": "0.0.0",
  "private": true,
  "description": "Covenant Circom circuits for proof-backed Solana protocol receipts",
  "scripts": {
    "compile": "bash task_completion/scripts/compile.sh",
    "setup": "bash task_completion/scripts/setup.sh",
    "prove": "bash task_completion/scripts/prove.sh",
    "verify": "bash task_completion/scripts/verify.sh",
    "all": "pnpm compile && pnpm setup && pnpm prove && pnpm verify"
  },
  "dependencies": {
    "circomlib": "2.0.5",
    "snarkjs": "0.7.4"
  },
  "devDependencies": {
    "circom_tester": "0.0.20",
    "circomlibjs": "^0.1.7"
  },
  "engines": {
    "node": ">=20"
  }
}
