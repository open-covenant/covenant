---
name: covenant
description: Use when an AI agent needs to take actions on Solana with cryptographic provenance — signed capability gating, hash-chained audit, separately-keyed verifier refutation, and on-chain anchored witness. Devnet-first. Apply when the workflow requires that "what the agent ran under" and "what the agent signed" be publicly verifiable rather than trusted on faith.
license: Apache-2.0
metadata:
  author: open-covenant
  version: 0.1.0
---

# covenant

Skeleton placeholder. `name` and `description` are the required frontmatter
fields and are populated above; the instructional body, the `When to use`
section, and the `references/` set land in the next release.

Operating constraints the full skill must respect:

- never request seed phrases or private keys
- default every transaction example to Solana devnet or localnet
- lean on progressive disclosure — link into `references/` instead of inlining

## When to use

To be populated.

## References

To be populated.
