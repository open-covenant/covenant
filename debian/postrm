#!/bin/sh
# Unit disable/purge handling is debhelper-generated (dh_installsystemd); the
# explicit maintscript keeps the scriptlet surface reviewable. State under
# /var/lib/covenant is operator data and is deliberately not purged here.
set -e

#DEBHELPER#

exit 0
