# Memory Drift Reports

`covenant verify` is a read-only consistency check across memory, audit, capability, settlement, and budget state. It does not mutate records. Add `--json` to emit one stable `verify_report` object for supervisors.

The verifier returns two layers:

- `checks`: aggregate pass/fail rows suitable for humans and dashboards.
- `drift`: machine-readable repair candidates with a kind, optional id, evidence message, and safe next repair action.

## Drift Kinds

| Kind | Meaning |
| --- | --- |
| `memory_without_audit` | A memory record has no matching `IntentDispatched` audit row. |
| `audit_without_memory` | An `IntentDispatched` audit row has no matching memory record in the sampled window. |
| `intent_dispatched_duplicate` | Two or more `IntentDispatched` audit rows share the same `intent_id`. |
| `memory_stale_parent` | A memory record points at a parent memory id that no longer exists. |
| `memory_self_parent` | A memory record's parent reference points at the record's own id. |
| `memory_parent_cycle` | A memory record's parent chain loops back on itself two or more hops up (e.g. A → B → A). |
| `capability_without_audit` | A capability grant exists without a matching `CapabilityGranted` audit row. |
| `memory_receipt_mismatch` | Memory records and memory settlement receipts differ for an owner in the sampled window. |
| `memory_without_receipt` | A memory record has no exact or legacy-compatible settlement receipt. |
| `receipt_without_memory_record` | A settlement receipt references a missing memory record. |
| `memory_receipt_duplicate` | More than one settlement receipt references the same memory record. |
| `memory_receipt_owner_mismatch` | A receipt references a memory record owned by a different payer. |
| `memory_receipt_resource_mismatch` | A settlement receipt carries `memory_record_id` but reports a non-Memory `resource`. |
| `memory_receipt_settled_before_created` | A settlement receipt's `settled_at` precedes the correlated memory record's `created_at`. |
| `budget_debit_paired_receipt_payer_mismatch` | A `BudgetDebit` joins by `paired_receipt` to a `ResourceKind::Tool` settlement receipt whose `payer` differs from the debit's `agent`. `record_paid_call` charges the debit and records the Tool receipt from one payer under a shared `receipt_id`, so a divergence misattributes a paid call's spend to the wrong agent's budget. Scoped to Tool receipts: memory-dispatch debits pair with `ResourceKind::Memory` receipts whose payer is the submitting peer rather than the debited agent, and are excluded by design. |
| `budget_debit_paired_receipt_payer_display_mismatch` | A `BudgetDebit` joins by `paired_receipt` to a `ResourceKind::Tool` settlement receipt whose `payer.pubkey` equals the debit's `agent.pubkey` while their `display` labels differ. `record_paid_call` clones one `payer` AgentId into both the debit's stored `agent` and the receipt's `payer` under one `receipt_id`, so a clean Tool pair carries byte-identical `payer.display`. The display half of the payer binding: the pubkey arm passes a display-only relabel (the keys agree), the budget ledger has no within-row shape arm, and the display is well-formed by the `AgentId` Deserialize gate, so this cross-store binding is the sole detector of a relabel that misattributes a paid call's spend to a differently-named identity while preserving the cryptographic key. Fires only when the pubkeys are equal and non-zero, so it is strictly disjoint from the pubkey-mismatch and `receipt_payer_pubkey_zeroed` arms. |
| `budget_debit_paired_receipt_credits_mismatch` | A `BudgetDebit` joins by `paired_receipt` to a `ResourceKind::Tool` settlement receipt whose `credits_consumed` differs from the debit's `credits`. `record_paid_call` sources both from one `call.credits`, so a divergence is a one-sided repricing that desyncs the per-payer burn the budget bucket enforces from the credits the settlement log accounts for. |
| `budget_debit_paired_memory_receipt_credits_not_intent_dispatch` | A `BudgetDebit` joins by `paired_receipt` to a `ResourceKind::Memory` settlement receipt but its `credits` is not the flat `intent_dispatch_credits()` (the v0 per-intent cost). `dispatch_intent_run` is the sole writer of that pairing and always debits exactly that constant, independent of the receipt's `credits_consumed = memory_write_credits(bytes)`, so a non-flat charge is a one-sided repricing of the memory-dispatch budget burn. |
| `budget_debit_paired_receipt_shared_by_multiple_debits` | More than one `BudgetDebit` carries the same non-nil `paired_receipt`. The pairing between a budget debit and its settlement receipt is 1:1: both production `try_debit` callers (`record_paid_call`, `dispatch_intent_run`) mint a fresh `receipt_id = Uuid::new_v4()` and debit it exactly once, and compaction only emits `Snapshot` rows while dropping pre-cutoff debits, so a repeated `paired_receipt` is a replayed or duplicated budget-log row, a forged debit reusing a live `receipt_id`, or a serde regression — a second charge against a receipt that already settled one debit, double-counting against the per-payer budget bucket. The budget ledger has no within-row shape arm, so this cross-row cardinality check is the sole detector. |
| `memory_empty_text` | A memory record's `text` is empty; the record cannot anchor retrieval and usually indicates a tool emitter that dropped its result body. |
| `memory_nan_embedding` | A memory record's `embedding` contains NaN values; cosine similarity poisons every ranking the record competes in. |
| `memory_record_id_nil` | A memory record has `id == Uuid::nil()` (the all-zero UUID). Every production memory write allocates the id via `Uuid::new_v4()` (either flowing through from `Intent.id` at IPC ingest or freshly allocated at write time), which never produces the nil UUID. A nil id is therefore evidence of a serde regression (`Uuid::default()` is nil), an import tool that constructed records without `Uuid::new_v4()`, or an operator SQLite edit that broke the `memory_record_id` back-reference settlement receipts and audit `IntentDispatched` rows correlate on. |
| `memory_record_created_at_zero` | A memory record has `created_at == 0`. Every production memory write stamps `created_at` from `epoch_ms()` (either flowing through `Intent.issued_at` — itself `epoch_ms()` at IPC ingest — or a fresh `epoch_ms()` at write time), which returns 0 only when the system clock predates 1970-01-01 (impossible). A zero `created_at` is therefore evidence of a serde regression, an import tool that bypassed `epoch_ms()`, or an operator SQLite edit that anonymized when the record was written. |
| `memory_record_owner_pubkey_zeroed` | A memory record has `owner.pubkey == [0u8; 32]` (the all-zero ed25519 verifying key). Every production memory write sources `owner.pubkey` from either an authenticated peer's `AgentId` (`issuer.clone()` on the intent path) or from `LocalIdentity::pubkey_bytes` (operator-initiated writes). ed25519 verifying keys are never the all-zero sequence, and a peer whose token resolved to a zeroed pubkey would fail signature verification on every capability check. A zeroed owner pubkey is therefore evidence of a serde regression (`Default` for `[u8; 32]` is all-zero), an import or replay tool that constructed records with a placeholder `AgentId`, or an operator SQLite edit that broke the owner-based scope, capability, and retrieval routing the daemon relies on. |
| `receipt_confirmed_without_chain` | A settlement receipt carries `confirmed_at` but `chain` is unset; only `annotate_receipt` writes `confirmed_at`, and it always sets `chain` from the same confirmation. |
| `receipt_chain_partial` | A settlement receipt has a strict subset (1-3 of 4) of the `chain`/`cluster`/`batch_id`/`merkle_root` bundle set; `annotate_receipt` writes the bundle as a unit. |
| `receipt_tx_sig_onchain_sig_diverged` | A settlement receipt has both `tx_sig` and `onchain_sig` populated but the values disagree; `annotate_receipt` writes both fields from the same `confirmation.tx_sig.clone()`, so a divergence is out-of-band. `Some`+`None` in either direction is tolerated for legacy/forward compatibility. |
| `receipt_settled_at_zero` | A settlement receipt has `settled_at == 0`. Every production receipt write stamps `settled_at` via `epoch_ms()`, which returns 0 only when the system clock predates 1970-01-01 (impossible). A zero `settled_at` is therefore evidence of a serde regression, a writer that bypassed `epoch_ms()`, or an operator JSONL edit that anonymized when the receipt was issued. The settlement-receipt JSONL has no chain-hash anchor covering this invariant. |
| `receipt_id_nil` | A settlement receipt has `id == Uuid::nil()` (the all-zero UUID). Every production receipt write allocates the id via `Uuid::new_v4()`, which never produces the nil UUID. A nil id is therefore evidence of a serde regression (`Uuid::default()` is nil), an import or replay tool that constructed receipts without `Uuid::new_v4()`, or an operator JSONL edit that broke the memory-record `metadata.receipt_id` back-reference and chain-batch correlation, both of which key on the receipt id. |
| `receipt_id_duplicate` | Two or more settlement receipts share the same non-nil `id`. Every production receipt is written with `id == Uuid::new_v4()` (`record_paid_call` for the Tool receipt, `dispatch_intent_run` for the Memory receipt), `JsonlReceiptStore::record` appends each receipt as one persisted line without deduplication, and the settlement-receipt backfill mutator rewrites the store one-to-one while preserving each id, so a non-nil receipt id is carried by exactly one receipt in faithful data. A repeated id is therefore evidence of a replayed or duplicated settlement-log row, an import tool that reused a prior id, or a serde regression. The `receipts_by_id` index the cross-record arms build collapses a duplicate to one entry, so this cardinality pass over the raw receipts is the sole detector — a duplicate id makes the receipt ambiguous to the `ExternalPaymentSettled` audit join and the budget-debit `paired_receipt` pairing that reference receipts by id. The nil UUID is skipped — `receipt_id_nil` owns it per row. |
| `receipt_payer_pubkey_zeroed` | A settlement receipt has `payer.pubkey == [0u8; 32]` (the all-zero ed25519 verifying key). Every production receipt write sources `payer.pubkey` from either an authenticated peer's `AgentId` (`issuer.clone()` on the intent settlement path) or from `LocalIdentity::pubkey_bytes` (daemon-initiated settlement). ed25519 verifying keys are never the all-zero sequence, and a peer whose token resolved to a zeroed pubkey would fail signature verification on every capability check. A zeroed payer pubkey is therefore evidence of a serde regression (`Default` for `[u8; 32]` is all-zero), an import or replay tool that constructed receipts with a placeholder `AgentId`, or an operator JSONL edit that broke the payer-based credit accounting, per-peer settlement queries, and on-chain bridge correlation the daemon relies on. |
| `audit_event_timestamp_zero` | An audit event has `timestamp_ms == 0`. Every production audit write goes through `epoch_ms()`, which returns 0 only when the system clock predates 1970-01-01 (impossible). A zero timestamp is therefore evidence of a serde regression, a writer that bypassed `epoch_ms()`, or an operator JSONL edit that anonymized when the event was recorded. The `AuditIntegrityReport` chain hash covers byte-tampering of the persisted file, not this semantic invariant. |
| `audit_event_id_nil` | An audit event has `id == Uuid::nil()` (the all-zero UUID). Every production audit write allocates the id via `Uuid::new_v4()`, which never produces the nil UUID. A nil id is therefore evidence of a serde regression (`Uuid::default()` is nil), an import or replay tool that constructed events without `Uuid::new_v4()`, or an operator JSONL edit that broke cross-event correlation by id. The chain hash protects byte integrity of the row, not the semantic invariant that an event id is unique and addressable. |
| `audit_event_id_duplicate` | Two or more audit events share the same non-nil `id`. Every production audit write allocates `id` via `Uuid::new_v4()` and `JsonlAuditLog::record` appends each event as one persisted line without deduplication, so a given non-nil event id is carried by exactly one row in faithful data. A repeated id is therefore evidence of a replayed or duplicated audit-log row (a crash-recovery or import tool re-appending an event), a replay tool that reused a prior id instead of allocating `Uuid::new_v4()`, or a serde regression that hydrated two rows with the same id. The `AuditIntegrityReport` chain hash covers byte tampering of each line but not this cross-row uniqueness invariant, so a duplicate id is the sole signal that the drift, capability, and memory correlation tables that reference audit events by id now resolve one id to two distinct events. The nil UUID is skipped here — `audit_event_id_nil` owns it per row. |
| `audit_event_issuer_pubkey_zeroed` | An audit event has `issuer.pubkey == [0u8; 32]` (the all-zero ed25519 verifying key). Every production audit write sources `issuer.pubkey` either from `LocalIdentity::pubkey_bytes` (daemon-issued events) or from an authenticated peer's `AgentId` (peer-issued events). ed25519 verifying keys are never the all-zero sequence, and a peer whose token resolves to a zeroed pubkey would fail signature verification on every capability check. A zeroed issuer pubkey is therefore evidence of a serde regression (`Default` for `[u8; 32]` is all-zero), an import or replay tool that constructed events with a placeholder `AgentId`, or an operator JSONL edit that anonymized issuer attribution and collapsed incident-response correlation to one identity. |
| `capability_action_empty` | A capability grant has an empty `action` string. Every production capability grant routes through `grant_capability`, which builds the `Capability` struct from a non-empty caller-supplied action (e.g. `memory.read`, `tool.call.web`, `a2a.send`). `validate_scope` short-circuits to Ok for empty actions because `ScopeNamespace::from_action` returns `None` on no-prefix match, so an empty-action capability still signs and persists in `granted.jsonl`. The cap is a no-op at use time — capability checks compare against required action strings that are always non-empty — but the row is structural evidence of a serde regression (`String::default()` is empty), a CLI bug that forwarded an empty `--action`, or a JSONL edit that anonymized the action while preserving the signature. |
| `capability_subject_pubkey_zeroed` | A capability grant has `subject.pubkey == [0u8; 32]` (the all-zero ed25519 verifying key). Every production capability grant sources `subject.pubkey` from either an authenticated peer's `AgentId` (`peer.clone()` at grant dispatch) or from `LocalIdentity::pubkey_bytes` (operator self-grants). ed25519 verifying keys are never the all-zero sequence, and a peer whose token resolved to a zeroed pubkey would fail signature verification on every transport check before reaching `grant_capability`. A zeroed subject pubkey is therefore evidence of a serde regression (`Default` for `[u8; 32]` is all-zero), an import or replay tool that constructed grants with a placeholder `AgentId`, or a JSONL edit that anonymized the subject so per-peer capability scope queries collapse zeroed subjects into one bucket. |
| `capability_grantor_pubkey_zeroed` | A capability grant has `granted_by.pubkey == [0u8; 32]` (the all-zero ed25519 verifying key). Every production capability grant sources `granted_by` from `self.identity.agent_id()` at grant dispatch — the daemon's local trust root, whose `AgentId.pubkey` is the 32-byte ed25519 verifying key derived from the daemon's signing key. ed25519 verifying keys are never the all-zero sequence, and `verify_with_clock` rejects any cap whose `granted_by` does not match the daemon trust root as `UntrustedGrantor`. A zeroed grantor pubkey is therefore evidence of a serde regression (`Default` for `[u8; 32]` is all-zero), an import tool that constructed grants without `self.identity.agent_id()`, or a JSONL edit that detached the row from the trust root, collapsing capability provenance across operator rotations and breaking incident-response correlation by grantor. |
| `capability_expires_at_zero` | A capability grant has `expires_at == Some(0)`. The cap is permanently expired before any check window — `verify_with_clock` rejects any cap whose `expires_at` is in the past — and no production grant path records this value (callers pass `None` for perpetual caps or `Some(future_epoch_ms)` for time-bounded caps). A `Some(0)` expiry is therefore evidence of a serde regression (e.g. `expires_at` rewritten as `u64` with `#[serde(default)]`), a CLI bug that defaulted a missing `--expires-at` to `Some(0)`, or a JSONL edit that disabled the cap without going through `revoke_capability` (which would record a `CapabilityRevoked` audit row). |
| `capability_signature_zeroed` | A capability grant has `signature == [0u8; 64]` (the all-zero ed25519 signature). Every production capability grant routes through `sign_capability`, which signs the canonical capability bytes with the daemon's ed25519 signing key. ed25519 signatures are never the all-zero 64-byte sequence — even an empty message signed by a real key produces non-zero bytes — and `verify_with_clock` would reject an all-zero signature at every capability check. An all-zero signature is therefore evidence of a serde regression (`Default` for `[u8; 64]` is all-zero), an import tool that skipped `sign_capability`, or a JSONL edit that wiped the signature to detach the row from the trust root. Co-fires `capability_without_audit` because the zeroed signature has no matching `CapabilityGranted.signature_b58` audit row. |
| `audit_capability_granted_signature_b58_empty` | An audit event has `kind == AuditKind::CapabilityGranted { signature_b58: "", .. }`. Every production `CapabilityGranted` audit write builds the variant with `signature_b58` sourced from `bs58::encode(signed.signature).into_string()`, where `signed.signature` is the 64-byte ed25519 signature produced by `sign_capability`. base58-encoding 64 bytes always produces a non-empty string. An empty `signature_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row before computing the signature, or a JSONL edit that wiped the trust-root binding from the audit row. The empty value detaches the row from the `signature_b58` join key used by the `capability ↔ audit` correlation, masking otherwise-orphaned capability rows behind a row whose top-level fields (`id`, `timestamp_ms`, `issuer`) all look valid. |
| `audit_capability_revoke_rejected_signature_b58_empty` | An audit event has `kind == AuditKind::CapabilityRevokeRejected { signature_b58: "", .. }`. Every production `CapabilityRevokeRejected` audit row is written from `revoke_capability`, which decodes the request's `signature_b58` via `bs58::decode` and only reaches the audit write after the decoded byte slice has exactly 64 entries. The recorded `signature_b58` is therefore the original non-empty base58 string of a 64-byte ed25519 signature. An empty `signature_b58` is evidence of a serde regression, an import tool that constructed the row without the original decoded signature, or a JSONL edit that wiped the join key — detaching the rejection row from the matching `CapabilityGranted` row and breaking every cap-grant ↔ revoke-rejection correlation. |
| `receipt_memory_record_id_nil` | A settlement receipt has `memory_record_id == Some(Uuid::nil())`. Production receipt writes either pass `None` for resource paths with no associated memory record, or `Some(memory_id)` where `memory_id` is allocated via `Uuid::new_v4()` at the matching memory write. `Uuid::new_v4()` never produces the nil UUID. A `Some(Uuid::nil())` is therefore evidence of a serde regression (`Uuid::default()` is nil), an import or replay tool that constructed receipts with a placeholder Uuid, or a JSONL edit that detached the row from the receipt ↔ memory record correlation while preserving payer, settled_at, and credits_consumed so the row still accounts for the credit burn. Co-fires nothing else — `receipt_id_nil` checks `receipt.id`, not `receipt.memory_record_id`. |
| `audit_intent_dispatched_result_hash_hex_empty` | An audit event has `kind == AuditKind::IntentDispatched { result_hash_hex: "", .. }`. Every production `IntentDispatched` audit write sources `result_hash_hex` from `covenant_audit::hash_hex(text_out.as_bytes())`, which produces a 64-character SHA-256 hex string and never the empty string. An empty `result_hash_hex` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that reconstructed the row without the original hash, or a JSONL edit that wiped the join key used to correlate the dispatch row with the `SettlementReceipt` that pins the same `text_out`. The empty value detaches the row from the `result_hash_hex` join key used by the `audit ↔ receipt` correlation. |
| `audit_hermes_tool_invoked_preview_hash_hex_empty` | An audit event has `kind == AuditKind::HermesToolInvoked { preview_hash_hex: "", .. }`. Every production `HermesToolInvoked` audit write sources `preview_hash_hex` from `covenant_audit::hash_hex(preview.as_bytes())` in `runtime_trace_to_audit_kind`, enforcing the documented redaction floor that Hermes tool-input previews are SHA-256-hashed before the audit chain persists them. SHA-256 hex is 64 lowercase characters and never empty. An empty `preview_hash_hex` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that reconstructed the row without the original hash, or a JSONL edit that wiped the redaction marker. The empty value erases the redaction binding and detaches the row from any audit-chain consumer that uses `preview_hash_hex` to verify the redaction floor. |
| `audit_intent_dispatched_intent_id_nil` | An audit event has `kind == AuditKind::IntentDispatched { intent_id: Uuid::nil(), .. }`. Every production `IntentDispatched` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed through the intent dispatch path, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the dispatch row from the Hermes step trail (`HermesToolInvoked`, `HermesToolCompleted`, `HermesApprovalRequested`, `HermesApprovalResolved`, `HermesFileWritten`) and breaks every `IntentDispatched ↔ Hermes-step` correlation that uses `intent_id` as the lookup. |
| `audit_hermes_tool_invoked_intent_id_nil` | An audit event has `kind == AuditKind::HermesToolInvoked { intent_id: Uuid::nil(), .. }`. Every production `HermesToolInvoked` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed in by the outer intent dispatch path through `runtime_trace_to_audit_kind`, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the Hermes-step row from the matching `AuditKind::IntentDispatched` row and breaks every `HermesToolInvoked ↔ IntentDispatched` correlation that uses `intent_id` as the lookup. |
| `audit_hermes_tool_completed_intent_id_nil` | An audit event has `kind == AuditKind::HermesToolCompleted { intent_id: Uuid::nil(), .. }`. Every production `HermesToolCompleted` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed in by the outer intent dispatch path through `runtime_trace_to_audit_kind`, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the completion row from the matching `AuditKind::IntentDispatched` row and from the paired `AuditKind::HermesToolInvoked` row, breaking both the `HermesToolCompleted ↔ IntentDispatched` and `HermesToolCompleted ↔ HermesToolInvoked` correlations that use `intent_id` as the lookup. |
| `audit_hermes_approval_requested_intent_id_nil` | An audit event has `kind == AuditKind::HermesApprovalRequested { intent_id: Uuid::nil(), .. }`. Every production `HermesApprovalRequested` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed in by the outer intent dispatch path through `runtime_trace_to_audit_kind`, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the approval-prompt row from the matching `AuditKind::IntentDispatched` row and from any later `AuditKind::HermesApprovalResolved` row that completes the pending approval, breaking both the `HermesApprovalRequested ↔ IntentDispatched` and `HermesApprovalRequested ↔ HermesApprovalResolved` correlations that use `intent_id` as the lookup. |
| `audit_hermes_approval_resolved_intent_id_nil` | An audit event has `kind == AuditKind::HermesApprovalResolved { intent_id: Uuid::nil(), .. }`. Every production `HermesApprovalResolved` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed in by the outer intent dispatch path through `runtime_trace_to_audit_kind`, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the approval-outcome row from the matching `AuditKind::IntentDispatched` row and from the paired `AuditKind::HermesApprovalRequested` row that opened the prompt, breaking both the `HermesApprovalResolved ↔ IntentDispatched` and `HermesApprovalResolved ↔ HermesApprovalRequested` correlations that use `intent_id` as the lookup. |
| `audit_hermes_file_written_intent_id_nil` | An audit event has `kind == AuditKind::HermesFileWritten { intent_id: Uuid::nil(), .. }`. Every production `HermesFileWritten` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier passed in by the outer intent dispatch path through `runtime_trace_to_audit_kind`, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the workspace-write row from the matching `AuditKind::IntentDispatched` row and from the rest of the Hermes step trail (`HermesToolInvoked`, `HermesToolCompleted`, `HermesApprovalRequested`, `HermesApprovalResolved`) that joins on the same `intent_id`, breaking every `HermesFileWritten ↔ IntentDispatched` and `HermesFileWritten ↔ Hermes-step` correlation that uses `intent_id` as the lookup. |
| `audit_intent_ignored_intent_id_nil` | An audit event has `kind == AuditKind::IntentIgnored { intent_id: Uuid::nil(), .. }`. Every production `IntentIgnored` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier the intent dispatch path produces alongside the `IntentResult` response, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the skip-rule rejection row from the matching `AuditKind::IntentDispatched` row, breaking every `IntentIgnored ↔ IntentDispatched` correlation that uses `intent_id` as the lookup. |
| `audit_budget_exhausted_intent_id_nil` | An audit event has `kind == AuditKind::BudgetExhausted { intent_id: Uuid::nil(), .. }`. Every production `BudgetExhausted` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier the outer intent-dispatch path produces alongside the matching `IntentDispatched` row, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the budget-rejection row from the matching `AuditKind::IntentDispatched` row and from `covenant intents resume <intent-id>`, breaking every `BudgetExhausted ↔ IntentDispatched` and `BudgetExhausted ↔ resume-queue` correlation that uses `intent_id` as the lookup. |
| `audit_budget_preempted_intent_id_nil` | An audit event has `kind == AuditKind::BudgetPreempted { intent_id: Uuid::nil(), .. }`. Every production `BudgetPreempted` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier the outer `preempt_intent` caller path receives alongside the matching `IntentDispatched` row, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the forced-termination row from the matching `AuditKind::IntentDispatched` row, breaking every `BudgetPreempted ↔ IntentDispatched` correlation that uses `intent_id` as the lookup and erasing the operator's post-mortem trail (`agent_display`, `reason`, `signal_sent`, `exit_code`) for forced subprocess terminations. |
| `audit_budget_preempt_failed_intent_id_nil` | An audit event has `kind == AuditKind::BudgetPreemptFailed { intent_id: Uuid::nil(), .. }`. Every production `BudgetPreemptFailed` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier the outer `preempt_intent` caller path receives alongside the matching `IntentDispatched` row, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the signal-send-failure row from the matching `AuditKind::IntentDispatched` row, breaking every `BudgetPreemptFailed ↔ IntentDispatched` correlation that uses `intent_id` as the lookup and erasing the incident-triage trail (`agent_display`, `reason`, `errno`) that classifies ESRCH (benign — subprocess already exited) vs. EPERM (security incident — daemon lacks signal-send permission) for the offending intent. |
| `audit_budget_unseeded_intent_id_nil` | An audit event has `kind == AuditKind::BudgetUnseeded { intent_id: Uuid::nil(), .. }`. Every production `BudgetUnseeded` audit write sources `intent_id` from the `Uuid::new_v4()`-allocated identifier the outer intent-dispatch path produces alongside the matching `IntentDispatched` row, and `Uuid::new_v4()` never produces the nil UUID. A nil `intent_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the operator-misconfig fail-open row from the matching `AuditKind::IntentDispatched` row, breaking every `BudgetUnseeded ↔ IntentDispatched` correlation that uses `intent_id` as the lookup and erasing the audit trail an operator uses to find which manifest opted into budget enforcement (`budget_credits_per_hour > 0`) without a matching `register_agent_budgets` seed call. |
| `audit_memory_repair_applied_memory_id_nil` | An audit event has `kind == AuditKind::MemoryRepairApplied { memory_id: Uuid::nil(), .. }`. Every production `MemoryRepairApplied` audit write sources `memory_id` from `outcome.id` returned by `memory.repair`, where `outcome.id` is the `Uuid::new_v4()`-allocated identifier of the repaired memory record, and `Uuid::new_v4()` never produces the nil UUID. A nil `memory_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the repair row from the affected memory record, breaking every `MemoryRepairApplied ↔ memory-record` correlation that uses `memory_id` as the lookup and erasing the audit trail an operator uses to trace which records were edited under each repair action (`delete`, `redact`, `mark_stale`, `detach_parent`). |
| `audit_external_payment_settled_receipt_id_nil` | An audit event has `kind == AuditKind::ExternalPaymentSettled { receipt_id: Uuid::nil(), .. }`. Every production `ExternalPaymentSettled` audit write sources `receipt_id` from `let receipt_id = Uuid::new_v4()` at the top of `record_paid_call` in covenantd `x402.rs`, and `Uuid::new_v4()` never produces the nil UUID. A nil `receipt_id` is therefore evidence of a serde regression (`Uuid::default()` is nil), an audit import tool that reconstructed the row with a placeholder, or a JSONL edit that wiped the cross-event join key. The nil value detaches the x402 paid-call row from the matching `SettlementReceipt`, breaking every `ExternalPaymentSettled ↔ SettlementReceipt` correlation that joins on `receipt_id` and erasing the audit trail an operator uses to trace a paid call back to the settlement receipt that recorded the credit debit. |
| `audit_authentication_failed_transport_empty` | An audit event has `kind == AuditKind::AuthenticationFailed { transport: "", .. }`. Every production `AuthenticationFailed` audit write reaches `record_auth_failure` with `transport` as a string literal: `"ipc"` at the IPC first-frame-must-be-Authenticate and unknown-token sites in the daemon connection loop, or `"http"` at the missing-Authorization-header and peer-registry-unavailable sites in the HTTP gateway. A string literal is never empty. An empty `transport` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original literal, or a JSONL edit that anonymized the surface attribution. The empty value detaches the rejection row from the surface-scoped audit feed filter at `ipc_audit_recent` (which keys on `transport.as_str()` to slice the per-surface view) and erases the IPC-vs-HTTP attribution an operator uses to triage probes against the correct transport. |
| `audit_peer_revoked_peer_pubkey_b58_empty` | An audit event has `kind == AuditKind::PeerRevoked { peer_pubkey_b58: "", .. }`. Every production `PeerRevoked` audit write sources `peer_pubkey_b58` from `bs58::encode(summary.agent_id.pubkey).into_string()` at the `revoke_peer` audit dispatch site, where `summary.agent_id.pubkey` is the 32-byte ed25519 verifying key of the revoked peer. base58-encoding 32 bytes always produces a non-empty string. An empty `peer_pubkey_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original encode (e.g. preserved `peer_display` and `token_prefix` but dropped the pubkey field), or a JSONL edit that anonymized the unforgeable identifier. The wire-supplied `peer_display` can collide across pubkeys (even with `validate_agent_id_display`), so `peer_pubkey_b58` is the authoritative join key for cross-rotation correlation; an empty value detaches the revocation row from every audit trail an operator uses to confirm which exact pubkey was revoked. |
| `audit_peer_revoked_token_prefix_empty` | An audit event has `kind == AuditKind::PeerRevoked { token_prefix: "", .. }`. Every production `PeerRevoked` audit write sources `token_prefix` from `summary.token_prefix` where `summary_from` sets `token_prefix = token_b58_prefix(&entry.token)`, and `token_b58_prefix` takes the first 6 chars of `token.to_b58()` of a 32-byte `PeerToken`. base58 of 32 non-zero bytes is ~44 chars long, so `chars().take(6)` always yields exactly 6 chars. An empty `token_prefix` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original 6-char redaction, or a JSONL edit that anonymized the token-correlation redaction. The empty value detaches the revocation row from the on-disk token file's first-6-char redaction (the same redaction `PeerToken::Debug` and the audit log's `*_token_prefix` fields publish) and breaks the rotation-then-revoke audit trail an operator uses to bind a revocation row to the `operator.token` file the redaction is designed to anchor. |
| `audit_operator_token_rotation_rejected_peer_pubkey_b58_empty` | An audit event has `kind == AuditKind::OperatorTokenRotationRejected { peer_pubkey_b58: "", .. }`. Every production `OperatorTokenRotationRejected` audit write sources `peer_pubkey_b58` from `bs58::encode(peer.pubkey).into_string()` at the `rotate_operator_token` audit dispatch site, where `peer.pubkey` is the authenticated rejected peer's 32-byte ed25519 verifying key. base58-encoding 32 bytes always produces a non-empty string. An empty `peer_pubkey_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original encode (e.g. preserved `peer_display` but dropped the pubkey field), or a JSONL edit that anonymized the unforgeable identifier. The wire-supplied `peer_display` is the rejected peer's display string and can collide across pubkeys; `peer_pubkey_b58` is the authoritative join key for rotation-gate probe triage on the operator's `/audit` feed. An empty value detaches the rejected-probe row from every audit-vs-identity correlation an operator uses to classify the probe (which exact pubkey attempted the unauthorized rotation). |
| `audit_operator_token_rotated_old_token_prefix_empty` | An audit event has `kind == AuditKind::OperatorTokenRotated { old_token_prefix: "", .. }`. Every production `OperatorTokenRotated` audit write sources `old_token_prefix` from `token_b58_prefix(&old_token)` at the `rotate_operator_token` success site, where `old_token` is the `PeerToken` parsed by `read_operator_token_b58` from the on-disk `operator.token` file. `token_b58_prefix` takes the first 6 chars of `token.to_b58()` of a 32-byte `PeerToken`, and base58 of 32 non-zero bytes is ~44 chars, so `chars().take(6)` always yields exactly 6 chars. An empty `old_token_prefix` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original 6-char redaction (e.g. preserved `peer_display` and `new_token_prefix` but dropped the prior-token field), or a JSONL edit that anonymized the prior-token redaction the rotation row exists to anchor. The empty value detaches the rotation row from the prior on-disk `operator.token` file's first-6-char redaction (the same redaction the subsequent `PeerRevoked` audit row publishes for the rotated-out token) and breaks the rotation-then-revoke audit trail an operator uses to bind the rotation row to the prior-token disk anchor. |
| `audit_operator_token_rotated_new_token_prefix_empty` | An audit event has `kind == AuditKind::OperatorTokenRotated { new_token_prefix: "", .. }`. Every production `OperatorTokenRotated` audit write sources `new_token_prefix` from `token_b58_prefix(&new_token)` at the `rotate_operator_token` success site, where `new_token` is the `PeerToken` returned by `PeerToken::generate()` (32 random bytes). `token_b58_prefix` takes the first 6 chars of `token.to_b58()` of a 32-byte `PeerToken`, and base58 of 32 non-zero bytes is ~44 chars, so `chars().take(6)` always yields exactly 6 chars. An empty `new_token_prefix` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original 6-char redaction (e.g. preserved `peer_display` and `old_token_prefix` but dropped the freshly-issued-token field), or a JSONL edit that anonymized the new-token redaction the rotation row exists to anchor for the post-rotation `operator.token` file. The empty value detaches the rotation row from the new on-disk `operator.token` file's first-6-char redaction (the same redaction the next `PeerRevoked` or rotation audit row will publish for this token) and breaks the rotation audit trail an operator uses to bind the rotation row to the freshly-issued-token disk anchor. |
| `audit_operator_peers_list_rejected_peer_pubkey_b58_empty` | An audit event has `kind == AuditKind::OperatorPeersListRejected { peer_pubkey_b58: "", .. }`. Every production `OperatorPeersListRejected` audit write sources `peer_pubkey_b58` from `bs58::encode(peer.pubkey).into_string()` at the `list_peers` reject path, where `peer.pubkey` is the authenticated rejected peer's 32-byte ed25519 verifying key. base58-encoding 32 bytes always produces a non-empty string. An empty `peer_pubkey_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original encode (e.g. preserved `peer_display` but dropped the pubkey field), or a JSONL edit that anonymized the unforgeable identifier. The wire-supplied `peer_display` is the rejected peer's display string and can collide across pubkeys; `peer_pubkey_b58` is the authoritative join key for peers-enumeration probe triage on the operator's `/audit` feed. An empty value detaches the rejected-probe row from every audit-vs-identity correlation an operator uses to classify the probe (which exact pubkey attempted the unauthorized peers list). |
| `audit_operator_peer_revoke_rejected_peer_pubkey_b58_empty` | An audit event has `kind == AuditKind::OperatorPeerRevokeRejected { peer_pubkey_b58: "", .. }`. Every production `OperatorPeerRevokeRejected` audit write sources `peer_pubkey_b58` from `bs58::encode(peer.pubkey).into_string()` at the `revoke_peer` non-operator reject path, where `peer.pubkey` is the authenticated rejected peer's 32-byte ed25519 verifying key. base58-encoding 32 bytes always produces a non-empty string. An empty `peer_pubkey_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original encode (e.g. preserved `peer_display` but dropped the pubkey field), or a JSONL edit that anonymized the unforgeable identifier. The wire-supplied `peer_display` is the rejected peer's display string and can collide across pubkeys; `peer_pubkey_b58` is the authoritative join key for peer-revoke probe triage on the operator's `/audit` feed. An empty value detaches the rejected-probe row from every audit-vs-identity correlation an operator uses to classify the probe (which exact pubkey attempted the unauthorized peer revoke). |
| `audit_peer_self_revoke_blocked_peer_pubkey_b58_empty` | An audit event has `kind == AuditKind::PeerSelfRevokeBlocked { peer_pubkey_b58: "", .. }`. Every production `PeerSelfRevokeBlocked` audit write sources `peer_pubkey_b58` from `bs58::encode(summary.agent_id.pubkey).into_string()` at the self-revoke-blocked path in `revoke_peer`, where `summary.agent_id.pubkey` is the 32-byte ed25519 verifying key of the operator-identity peer whose self-revoke was blocked. base58-encoding 32 bytes always produces a non-empty string. An empty `peer_pubkey_b58` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original encode (e.g. preserved `peer_display` and `token_prefix` but dropped the pubkey field), or a JSONL edit that anonymized the unforgeable operator-identity anchor. The wire-supplied `peer_display` is the operator's display string and can collide across pubkeys; `peer_pubkey_b58` is the authoritative join key for operator self-fat-finger audit triage. An empty value detaches the blocked-self-revoke row from the audit trail that distinguishes operator self-revoke probes from peer revoke probes. |
| `audit_peer_self_revoke_blocked_token_prefix_empty` | An audit event has `kind == AuditKind::PeerSelfRevokeBlocked { token_prefix: "", .. }`. Every production `PeerSelfRevokeBlocked` audit write sources `token_prefix` from `summary.token_prefix.clone()` at the self-revoke-blocked path in `revoke_peer`, where `summary.token_prefix` is set by `summary_from` to `token_b58_prefix(&entry.token)`. `token_b58_prefix` takes the first 6 chars of `token.to_b58()` of a 32-byte `PeerToken`, and base58 of 32 non-zero bytes is ~44 chars, so `chars().take(6)` always yields exactly 6 chars. An empty `token_prefix` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original 6-char redaction (e.g. preserved `peer_display` and `peer_pubkey_b58` but dropped the token-prefix field), or a JSONL edit that anonymized the on-disk token-file correlation redaction. The empty value detaches the blocked-self-revoke row from the `operator.token` file's first-6-char redaction (the same redaction `PeerToken::Debug` and every sibling `*_token_prefix` audit field publish) and breaks the rotation-then-self-revoke audit trail an operator uses to bind a blocked-self-revoke row to the `operator.token` file the redaction is designed to anchor. |
| `audit_a2a_result_rejected_reason_empty` | An audit event has `kind == AuditKind::A2AResultRejected { reason: "", .. }`. The only production `A2AResultRejected` write-site in covenantd is `post_a2a_result`'s `lookup_task_sender` `Ok(None)` branch, which always sets `reason` to the hardcoded literal `"unknown_task"`. No production code path ever sets the field to an empty string. An empty `reason` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original literal (e.g. preserved `task_id` but dropped the cause field), or a JSONL edit that anonymized the server-derived rejection cause. `A2AResultRejected` is a stronger compromise indicator than a missing-cap rejection because no honest agent generates a nonexistent `task_id`; the empty `reason` detaches the row from the load-bearing classification that distinguishes upstream-compromise probes from generic capability-check rejections, erasing the audit correlation chain an operator uses to triage the probe. |
| `audit_a2a_repair_applied_action_empty` | An audit event has `kind == AuditKind::A2ARepairApplied { action: "", .. }`. The two production `A2ARepairApplied` write-sites in covenantd are `repair_a2a_task` (operator-initiated repair path, which sets `action` from `a2a_repair_action(&request.command)` returning the hardcoded literals `"requeue"` or `"force_error"`) and `retry_a2a_stale` (scheduler-initiated retry path, which sets `action` to the hardcoded literal `"auto_requeue"`). No production code path ever sets the field to an empty string. An empty `action` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original dispatch literal (e.g. preserved `task_id`, `reason`, and `lease_id` but dropped the action field), or a JSONL edit that anonymized the dispatch classification. The empty value detaches the repair row from the source-path attribution that distinguishes scheduler-initiated `auto_requeue` activity from operator-initiated `requeue`/`force_error` mutations, erasing the load-bearing classification the repair-history correlation chain leans on to triage repair churn. |
| `audit_a2a_recipient_rejected_action_empty` | An audit event has `kind == AuditKind::A2ARecipientRejected { action: "", .. }`. The only production `A2ARecipientRejected` write-site in covenantd is `send_a2a_task`'s recipient admission gate, which sets `action` to `recv_display_action.clone()` where `recv_display_action = peer.scoped_action_alternatives("a2a.recv")[0] = format!("a2a.recv.{display}")`. The hardcoded `"a2a.recv."` prefix is 9 chars even when `display` is empty, so no production code path ever sets the field to an empty string. An empty `action` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original concatenation (e.g. preserved `sender_display` and `recipient_display` but dropped the action field), or a JSONL edit that anonymized the server-derived recipient-capability-action correlation key. The empty value detaches the rejection row from the `a2a.recv.<sender>` capability action shape whose grant the recipient was missing, erasing the load-bearing scope-action correlation key the missing-grant audit chain joins on when an operator triages A2A admission failures. |
| `audit_authentication_failed_reason_empty` | An audit event has `kind == AuditKind::AuthenticationFailed { reason: "", .. }`. Every production `AuthenticationFailed` write-site in covenantd passes `reason` as a hardcoded `&'static str` literal at `record_auth_failure` call-sites: IPC first-frame/rejected-auth paths use `"unknown or revoked token"` and `"first frame must be Authenticate"`; the HTTP storage-outage path uses `"peer registry unavailable"`; and the HTTP bearer-auth `reject()` helper forwards one of `"missing Authorization header"`, `"expected \`Authorization: Bearer <token>\`"`, `"malformed bearer token"`, or `"unknown or revoked token"`. No production code path ever sets the field to an empty string. An empty `reason` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original literal (e.g. preserved `transport` but dropped the cause field), or a JSONL edit that anonymized the server-derived rejection cause. The empty value detaches the rejection row from the cause classification that distinguishes credential-probe categories (missing header vs malformed bearer vs unknown token vs storage outage), erasing the audit signal an operator uses to triage authentication failures by root cause. |
| `audit_capability_grant_rejected_reason_empty` | An audit event has `kind == AuditKind::CapabilityGrantRejected { reason: "", .. }`. The only production `CapabilityGrantRejected` write-site in covenantd is `grant_capability`'s `validate_scope`-error branch, which sets `reason` to `e.to_string()` where `e` is a `PermissionError`. Every `PermissionError` variant has a non-empty `thiserror` Display format (`"io: ..."`, `"serde: ..."`, `"ed25519: ..."`, `"capability expired at ..."`, `"signature does not verify against granted_by pubkey"`, `"granted_by pubkey does not match the daemon trust root"`, `"invalid capability scope: ..."`). No production code path ever sets the field to an empty string. An empty `reason` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original `PermissionError` display (e.g. preserved `subject_display` and `action` but dropped the cause field), or a JSONL edit that anonymized the server-derived `PermissionError` classification. The empty value detaches the rejection row from the classification that distinguishes scope-validation probes (`invalid capability scope: ...`) from cryptographic failures (signature/trust failures), erasing the audit signal an operator uses to triage capability-grant failures by root cause. |
| `audit_intent_ignored_matched_pattern_empty` | An audit event has `kind == AuditKind::IntentIgnored { matched_pattern: "", .. }`. The only production `IntentIgnored` write-site in covenantd is `submit_intent`'s ignore-check branch, which sets `matched_pattern` to `ignore_check.matched.map(|p| p.raw().trim().to_string()).unwrap_or_default()`. When `ignore_check.ignored` is true, `IgnoreSet::check` guarantees `matched` is `Some(p)`, and `p` was constructed by `IgnorePattern::parse`, which rejects empty/whitespace/comment lines and patterns whose stripped glob is empty. `p.raw().trim()` is therefore always non-empty in production. An empty `matched_pattern` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original `.covenantignore` rule (e.g. preserved `intent_id` and `intent_text` but dropped the rule field), or a JSONL edit that anonymized the server-derived rule key. The empty value detaches the suppression row from the `.covenantignore` rule identifier that distinguishes which pattern fired, erasing the audit trail an operator uses to triage ignore-rule false positives or escalating suppression aggressiveness. |
| `audit_budget_preempted_signal_sent_empty` | An audit event has `kind == AuditKind::BudgetPreempted { signal_sent: "", .. }`. The only production `BudgetPreempted` write-site in covenantd is `preempt_intent`'s match on `PreemptOutcome`, where `signal_sent` is assigned one of three hardcoded `&'static str` literals via `.into()`: `"none"` for `PreemptOutcome::AlreadyDead`, `"SIGTERM"` for `PreemptOutcome::ExitedDuringGrace`, and `"SIGKILL"` for `PreemptOutcome::SigKilled`. No production code path ever sources the field from caller input, and no reachable arm assigns an empty string. An empty `signal_sent` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original signal classifier (e.g. preserved `intent_id` and `agent_display` but dropped the signal field), or a JSONL edit that anonymized the cooperative-vs-forced classification. The empty value detaches the forced-termination row from the operator's post-mortem distinction between a natural-exit grace path (`none`), a grace-window cooperative exit after `SIGTERM`, and a forced `SIGKILL` escalation, erasing the signal an operator uses to classify whether a preempted subprocess respected the grace window or required forced termination. |
| `audit_intent_dispatched_status_empty` | An audit event has `kind == AuditKind::IntentDispatched { status: "", .. }`. The only production `IntentDispatched` write-site in covenantd is the `dispatch_intent` success-path emission, which sets `status` to the hardcoded `&'static str` literal `"ok"` via `.into()`. No production code path ever sources the field from caller input; the ignored-by-`.covenantignore` path emits `AuditKind::IntentIgnored` (a distinct variant), not `IntentDispatched`. An empty `status` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original success literal (e.g. preserved `intent_id` and `intent_text` but dropped the status field), or a JSONL edit that anonymized the dispatch-outcome classifier. The empty value detaches the dispatch row from the success classifier that distinguishes a completed dispatch from any future failure-state extension, collapsing the load-bearing kind+status discriminator every dispatch-audit consumer joins on when filtering completed dispatches from concurrent `IntentIgnored` suppression rows. |
| `audit_memory_repair_applied_action_empty` | An audit event has `kind == AuditKind::MemoryRepairApplied { action: "", .. }`. The only production `MemoryRepairApplied` write-site in covenantd is `repair_memory`'s success branch, which sets `action` to `memory_repair_action(&request.command).into()` where `memory_repair_action` returns one of three hardcoded `&'static str` literals exhaustively over the `MemoryRepairCommand` enum: `"detach_parent"` (DetachParent), `"delete_record"` (DeleteRecord), or `"backfill_provenance"` (BackfillProvenance). No production code path ever sources the field from caller input. An empty `action` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original repair-command literal (e.g. preserved `memory_id` and `mode` but dropped the action field), or a JSONL edit that anonymized the server-derived `MemoryRepairCommand` classifier. The empty value detaches the repair row from the command classifier that distinguishes a parent-detachment from a record-deletion from a provenance-backfill, erasing the audit trail an operator uses to attribute a memory mutation to a specific `MemoryRepairCommand` variant. |
| `audit_memory_compaction_applied_mode_empty` | An audit event has `kind == AuditKind::MemoryCompactionApplied { mode: "", .. }`. The only production `MemoryCompactionApplied` write-site in covenantd is `compact_memory`'s success branch, which sets `mode` to `memory_repair_mode(request.mode).into()` where `memory_repair_mode` returns one of two hardcoded `&'static str` literals exhaustively over the `MemoryRepairMode` enum: `"dry_run"` (DryRun) or `"apply"` (Apply). No production code path ever sources the field from caller input. An empty `mode` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original mode literal (e.g. preserved `changed`, `reason`, and the id vectors but dropped the mode field), or a JSONL edit that anonymized the dry-run-vs-apply classifier. The empty value detaches the compaction row from the classifier that distinguishes a dry-run preview from an applied compaction mutation, collapsing the load-bearing mutation-vs-preview discriminator every compaction-audit consumer joins on when filtering applied compactions from preview rows. |
| `audit_memory_repair_applied_mode_empty` | An audit event has `kind == AuditKind::MemoryRepairApplied { mode: "", .. }`. The only production `MemoryRepairApplied` write-site in covenantd is `repair_memory`'s success branch, which sets `mode` to `memory_repair_mode(request.mode).into()` where `memory_repair_mode` returns one of two hardcoded `&'static str` literals exhaustively over the `MemoryRepairMode` enum: `"dry_run"` (DryRun) or `"apply"` (Apply). No production code path ever sources the field from caller input. An empty `mode` is therefore evidence of a serde regression (`String::default()` is empty), an audit import tool that constructed the row without the original mode literal (e.g. preserved `memory_id`, `action`, and `reason` but dropped the mode field), or a JSONL edit that anonymized the dry-run-vs-apply classifier. The empty value detaches the repair row from the classifier that distinguishes a dry-run preview from an applied repair mutation, collapsing the load-bearing mutation-vs-preview discriminator every repair-audit consumer joins on when filtering applied repairs from preview rows. |

## Operator Posture

The verifier is intentionally non-mutating. Drift is evidence, not an automatic delete instruction.

Safe handling order:

1. Inspect the drift item and the underlying state file.
2. Decide whether the record is valid, stale, missing provenance, or externally mutated.
3. Prefer the explicit repair commands (Repair Contract below) over ad hoc file edits.
4. Preserve useful long-term memory unless there is clear evidence it is stale or unsafe.

## Repair Contract

The memory crate defines explicit repair requests with two modes:

- `dry_run`: compute the exact before/after shape without mutating the store.
- `apply`: perform the mutation after the same checks pass.

Every repair request requires a non-empty reason. Supported crate-level commands are:

| Command | Use | Safety guard |
| --- | --- | --- |
| `detach_parent` | Clear a stale `parent` reference after inspection. | Optional `expected_parent` prevents detaching if the record changed since the drift report. |
| `delete_record` | Remove a memory record confirmed to be unsafe, invalid, or unwanted. | Dry-run reports the deletion without mutating; apply requires an explicit reason and capability. |
| `backfill_provenance` | Add provenance evidence under `metadata.provenance`. | Rejects null provenance payloads and preserves existing metadata. |

The daemon exposes the same repair request shape over IPC and HTTP `POST /memory/repair`. The CLI defaults to dry-run and requires `--apply` before mutation:

```bash
covenant capabilities grant memory.repair.dry_run
covenant memory repair detach-parent <memory-id> \
  --expected-parent <parent-id> \
  --reason "verified stale parent"

covenant capabilities grant memory.repair.apply
covenant memory repair backfill-provenance <memory-id> \
  --provenance '{"source":"audit-reconciliation"}' \
  --reason "verified missing provenance" \
  --apply
```

Dry-run calls require `memory.repair.dry_run`; apply calls require `memory.repair.apply`. Successful dry-runs and mutations record `memory_repair_applied` audit rows containing the memory id, action, mode, changed flag, and operator reason. Full before/after records stay in the repair response rather than being duplicated into the audit log.

## Compaction Contract

Compaction is separate from targeted repair. It is an operator-only maintenance request that computes one deterministic plan over the current memory snapshot. The same request shape is available over IPC, HTTP `POST /memory/compact`, and the CLI.

Supported policy fields:

| Field | Effect |
| --- | --- |
| `delete_working_before_ms` | Delete working-tier records older than the cutoff. |
| `delete_episodic_before_ms` | Delete episodic records older than the cutoff. |
| `mark_longterm_stale_before_ms` | Keep long-term records, but mark matching records under `metadata.stale_context`. |
| `detach_stale_parents` | Clear parent ids that do not resolve or are deleted by the same compaction plan. |
| `marked_at_ms` | Optional deterministic timestamp for stale-context markers; the daemon fills it when omitted. |

The CLI defaults to dry-run and requires `--apply` before mutation:

```bash
covenant capabilities grant memory.compact.dry_run
covenant memory compact \
  --delete-working-older-than-ms 86400000 \
  --detach-stale-parents \
  --reason "daily working-memory compaction"

covenant capabilities grant memory.compact.apply
covenant memory compact \
  --delete-working-older-than-ms 86400000 \
  --delete-episodic-older-than-ms 2592000000 \
  --mark-longterm-stale-older-than-ms 7776000000 \
  --detach-stale-parents \
  --reason "monthly memory hygiene" \
  --apply
```

The CLI prints a bare `MemoryCompactionOutcome` JSON object by default. Use `--json` for a stable envelope:

```bash
covenant memory compact --reason "monthly memory hygiene" --json ...
```

```json
{ "kind": "memory_compacted", "outcome": { "mode": "dry_run", "changed": false } }
```

Dry-run calls require `memory.compact.dry_run`; apply calls require `memory.compact.apply`. Successful dry-runs and mutations record `memory_compaction_applied` audit rows containing the mode, changed flag, operator reason, deleted ids, stale-marked ids, and detached-parent ids. Long-term memory is not deleted by compaction; it is marked stale so future retrieval policy can decide how to treat it.

## Receipt Correlation

Memory settlement receipts now carry an optional `memory_record_id` field that points at the originating `MemoryRecord.id` when daemon memory writes produce the receipt. `covenant verify` joins on that id first, then falls back to owner/resource counts only for older receipt rows that predate the field. Exact drift surfaces as `memory_without_receipt`, `receipt_without_memory_record`, `memory_receipt_duplicate`, or `memory_receipt_owner_mismatch`; aggregate count drift still surfaces as `memory_receipt_mismatch` when exact pairing is impossible.

The settlement crate now exposes a rollback-backed receipt backfill primitive for this migration boundary. `backfill_receipts_with_correlations` rewrites a receipt JSONL only after writing and fsyncing a sibling rollback checkpoint, then applies explicit `receipt_id -> memory_record_id` correlations and canonicalizes serde-decodable legacy rows. The convenience `backfill_receipts(path, dry_run)` wrapper performs only safe canonical row repair because the settlement crate cannot infer memory ids by itself.

The operator-facing path is now wired end-to-end on the memory side. `covenant memory backfill-receipt-correlation --json` (HTTP equivalent: `POST /memory/records/backfill`) applies by default; pass `--dry-run` to report the row_count an apply would change without writing. Dry-run requires the `memory.backfill.dry_run` capability; apply requires `memory.backfill.apply` — see [docs/capabilities.md](./capabilities.md) for the scope contract. The daemon recomputes correlations server-side from the operator's own memory and receipt rows; clients cannot supply correlations directly, which keeps a peer holding `memory.backfill.apply` from rewriting arbitrary `metadata.receipt_id` values by inventing pairings. A successful apply wraps the per-row updates in a SQLite SAVEPOINT named `backfill_receipt_correlation` so a per-row failure rolls the entire batch back to zero rows changed, then emits an operator-issued `MemoryRecordBackfillApplied` audit row carrying `row_count`, `savepoint_name`, and `dry_run`. The read-only verifier and `covenant memory repair` primitives remain the right tools for drift items outside the legacy-receipt-correlation surface (e.g., stale parent references, invalid records, missing provenance).
