# Capability Status

This status matrix separates what exists from what is experimental or planned. Update it when implementation or evidence changes.

| Capability | Status | Evidence | Next hardening step |
|---|---|---|---|
| Local daemon and CLI | Implemented, hardening | `agent-os/crates/covenantd`, `agent-os/crates/covenant`, daemon/CLI tests, CLI protocol metadata probe, privileged command live matrix with covered/deferred verb states | Fill deferred Solana transaction-preparation verbs after daemon signing and settlement deployment readiness exist. |
| IPC and HTTP gateway | Implemented, hardening | `covenant-ipc`, unauthenticated `protocol_info`, HTTP `/version`, v1 protocol fixture replay harness, v2 fixture staging guard, protected HTTP mutation live coverage, daemon and gateway tests, `docs/protocol-versioning.md` | Add concrete v2 fixtures and migration notes only with an intentional protocol bump. |
| Identity and peer auth | Implemented, hardening | `covenant-identity`, `covenant-peer-auth`, token rotation tests, local `identity-provenance` dry-run report with redacted peer-token rotation evidence, `docs/identity-provenance.md` | Human-approved public identity attestation custody and publication. |
| Signed capabilities | Implemented, hardening | `covenant-permissions`, grant-time scope validation for known namespaces, dispatch-time `tool.call.*` argument allowlists with live HTTP scoped-argument rejection coverage, dispatch-time `audit.purge` cutoff enforcement with live CLI rejection coverage, dispatch-time memory read/write/purge/repair/compaction scope enforcement, dispatch-time A2A send/recv/respond/repair scope enforcement with live delegated repair denial evidence, dispatch-time peer list/revoke/purge scope enforcement with scoped delegated `peers.revoke` live denial/allowance evidence, dispatch-time chain receipt read/batch/flush scope enforcement, capability enforcement tests, `docs/capabilities.md` scope contract | Keep broadening scoped delegated live coverage where policies are stable. |
| Audit log | Implemented, hardening | `covenant-audit`, daemon audit event tests, local hash-chain sidecar, CLI/HTTP/IPC integrity report, stable CLI integrity JSON envelope, unsigned and locally signed `audit-root-attestation.v1` generator/verifier, release-target audit-root subject binding fixture, ADR 0004 signing policy, `docs/provenance/audit-root-release-custody.md` | Add project key custody and release publication, then later transparency-log publication. |
| Memory store | Implemented, hardening | `covenant-memory`, SQLite, embedding tests, read-only drift reports with exact receipt correlation, scoped read/write/purge/repair/compaction dispatch checks, daemon/HTTP/CLI repair commands, bounded daemon/HTTP/CLI compaction commands, read-only `memory plan-compaction` and `memory plan-receipt-backfill` JSON planning, live compaction and receipt-backfill planning coverage, repair and compaction audit rows | Explicit receipt backfill mutation for legacy uncorrelated rows. |
| Runtime execution | Partially implemented | `covenant-runtime`, subprocess timeout and malformed-stdout tests, manifest sandbox requirement parsing, daemon backend selection, initial `runsc` OCI runner tests, opt-in live Linux gVisor dispatch test, documented Linux runner setup, `docs/gvisor-host-readiness.md`, `agent-os/scripts/gvisor-host-readiness.mjs`, `agent-os/scripts/validate-gvisor-host-readiness.mjs` | Promote a pinned Linux gVisor runner and rootfs into CI, then broaden sandbox policy enforcement. |
| MCP tools | Experimental | `covenant-mcp`, native/external transport tests, multi-tool stdio live compatibility with JSON content and tool-level error result parsing | Add an opt-in third-party MCP server fixture after fixture selection is stable and dependency provenance is pinned. |
| A2A messaging | Implemented, hardening | `covenant-a2a`, leased queue state, explicit task-kind metadata with legacy `intent_text` fallback, task idempotency metadata, receiver-side idempotency result cache, lease-age status filters, restart and CLI repair live tests, daemon/HTTP/CLI repair commands, disabled-by-default retry gate, opt-in background retry scheduler, repair and scheduler audit rows, per-peer repair report dry-run, delegated repair authorization policy, peer-mismatched scope denial daemon regression and live IPC coverage, ADR 0005 delegated repair release gate, `docs/a2a-repair-visibility.md`, `docs/a2a-repair-authorization.md`, `docs/decisions/0005-a2a-delegated-repair-release-review.md`, `agent-os/scripts/a2a-repair-visibility.mjs`, `agent-os/scripts/a2a-repair-release-review.mjs`, `agent-os/scripts/a2a-peer-repair-report.mjs`, `agent-os/scripts/validate-a2a-repair-release-review.mjs`, `agent-os/scripts/validate-a2a-repair-visibility.mjs` | Human release-review marker before delegated repair automation expands. |
| Budget ledger | Implemented, hardening | `covenant-budget`, persisted budget pause checkpoint records, daemon save/claim wiring, single-use resume claim tests, daemon budget tests, `covenant intents resume latest --json`, live CLI resume test, `docs/budget-pause-checkpoints.md` | Hard preemption of already-running subprocess work remains future runtime work. |
| Local settlement receipts | Implemented, hardening | `covenant-settlement`, receipt tests, memory-record correlation field, typed migration plan shape, read-only JSONL migration planner with malformed-row classification, `docs/settlement-receipt-migration.md` | Authorized receipt backfill mutation for legacy uncorrelated rows, with rollback and audit evidence. |
| On-chain settlement | Planned / scaffolded | `agent-os/programs/settlement`, `docs/on-chain-settlement-readiness.md`, `docs/settlement-oracle-policy.md`, `agent-os/scripts/settlement-oracle-policy.mjs`, `agent-os/scripts/validate-settlement-oracle-policy.mjs`, `agent-os/scripts/settlement-deployment-readiness.mjs`, `agent-os/scripts/validate-settlement-deployment-readiness.mjs` | Complete security review acceptance, production oracle source and update authority selection, mint authority custody, treasury rotation, and emergency-operation evidence. |
| Autonomous workflow | Experimental | `docs/autonomous-development.md`, `agent-os/autonomy`, validator, transition event log, sprint summary generator/publication/check validator, status gap report, unsigned review artifact scaffold/verifier/toolchain validator, signed review artifact verifier support behind explicit trusted public key input, autonomy environment preflight, dirty handoff report, handoff bundle export/verifier/restore plan, handoff toolchain validator, commit rotation policy validator, git identity validator, Git metadata write-access preflight, local pre-push guard | Project review signing key custody, public key publication, rotation, and release evidence policy. |
| Live boundary coverage | Experimental | `agent-os/autonomy/live-coverage.json`, `agent-os/scripts/validate-live-coverage.mjs`, opt-in `live_` tests, verifier drift/repair coverage, delegated A2A repair denial coverage, external-service gVisor coverage entry with promotion metadata, `docs/gvisor-live-runner.md`, `docs/gvisor-host-readiness.md` | Promote the documented Linux gVisor runner into CI once host provisioning is stable, and keep adding mutation-edge live tests where policies are stable. |
| Public provenance | Experimental | `agent-os/scripts/provenance.mjs`, `docs/provenance/attestations`, audit-root attestation signing/verification support, ADR 0004 audit-root signing policy, `docs/provenance/release-subjects.md`, `docs/provenance/release-readiness.md`, `agent-os/scripts/release-artifact-subject.mjs`, `agent-os/scripts/validate-release-artifact-subject.mjs`, `agent-os/scripts/release-provenance-readiness.mjs`, `agent-os/scripts/validate-release-provenance-readiness.mjs` | Implement project key custody, public key publication, release signing, and transparency publication. |
| Distribution and SDK ecosystem | Partially implemented, alpha contract defined | `docs/alpha-release-contract.md` defines a source-built alpha boundary, `docs/source-install.md` defines the local source installer, read-only upgrade preflight, and local rollback checkpoint contract, `agent-os/scripts/install-source.mjs` installs `covenantd` and `covenant` into a selected prefix with `covenant.source-install.v1` manifest output and rollback checkpoint metadata when replacing prior files, `agent-os/scripts/validate-source-installer.mjs` pins dry-run behavior, `agent-os/scripts/source-install-upgrade-plan.mjs` emits `covenant.source-install-upgrade-plan.v1` with fresh/clean/partial/drifted prefix classification, `agent-os/scripts/validate-source-install-upgrade-plan.mjs` tests fresh, clean existing, and drifted existing prefixes, `agent-os/scripts/source-install-rollback.mjs` verifies checkpoint digests, restores prior files, and writes local rollback evidence, `agent-os/scripts/validate-source-install-rollback.mjs` tests install, checkpoint, rollback, and tamper rejection, `docs/sdk-compatibility.md` defines workspace-alpha SDK compatibility policy, SDK export fixtures under `packages/sdk/compatibility` and `packages/sdk-ui/compatibility` pin source export drift, `packages/sdk/compatibility/instructions.v1.json` pins current Solana instruction descriptor names, account order, and data keys until generated bindings exist, `agent-os/scripts/sdk-compatibility.mjs` emits `covenant.sdk-compatibility.v1` with package metadata, export-map evidence, export fixture comparison, and instruction fixture comparison, `agent-os/scripts/validate-sdk-compatibility.mjs` pins workspace SDK readiness while keeping public SDK publication blocked, `docs/package-manager-readiness.md` defines package-manager readiness gates, `agent-os/scripts/package-manager-readiness.mjs` emits `covenant.package-manager-readiness.v1`, `agent-os/scripts/validate-package-manager-readiness.mjs` keeps public packages blocked until manifests, signing, CI, artifact hosting, and publication evidence exist, `docs/distribution-readiness.md` defines package-manager, signing, SDK stability, and upgrade graduation gates, `agent-os/scripts/distribution-readiness.mjs` emits `covenant.distribution-readiness.v1`, `agent-os/scripts/validate-distribution-readiness.mjs` pins the blocked-public-distribution state, `docs/releases/README.md` defines evidence bundles, `agent-os/scripts/alpha-release-evidence.mjs` emits `covenant.alpha-release-evidence.v1` with sanitized alpha readiness state, `agent-os/scripts/alpha-release-bundle.mjs` scaffolds local evidence bundles with `covenant.alpha-release-manifest.v1` digest manifests, `agent-os/scripts/alpha-release-validate-bundle.mjs` validates bundle acceptance gates including schema, evidence metadata binding, complete manifest file inventory, manifest digests, section-scoped gate outcomes, skipped-gate reasons, accepted-decision gate consistency, and section-scoped readiness blockers, `agent-os/scripts/validate-alpha-release-evidence.mjs` checks draft override and synthetic accepted-bundle paths, and `agent-os/scripts/alpha-release-readiness.mjs` reports repository-level alpha blockers including handoff and unsigned review-artifact toolchain readiness | Implement concrete Homebrew, Nix, Debian, and RPM manifests; release signing verification; generated protocol bindings from IDL; and package-manager rollback tests. |

## Validation Signals

- Default Rust gate: `bash agent-os/scripts/validate.sh`
- Scripts-only gate: `bash agent-os/scripts/validate.sh --scripts`
- Autonomy artifact gate: `node agent-os/scripts/validate-autonomy.mjs`
- Commit rotation policy: `node agent-os/scripts/validate-commit-rotation.mjs`
- Git identity guard: `node agent-os/scripts/validate-git-identity.mjs`
- Status evidence gate: `node agent-os/scripts/validate-status-evidence.mjs`
- Provenance gate: `node agent-os/scripts/provenance.mjs verify-all`
- Next task selector: `node agent-os/scripts/autonomy-next.mjs`
- Live coverage inventory: `bash agent-os/scripts/test-stats.sh`
- Live coverage matrix gate: `node agent-os/scripts/validate-live-coverage.mjs`
- A2A repair release-review gate: `node agent-os/scripts/validate-a2a-repair-release-review.mjs`
- Landing docs build: `pnpm --dir landing build`
- Dependency audit: `bash scripts/audit.sh`
- Alpha evidence gate: `node agent-os/scripts/validate-alpha-release-evidence.mjs`

Alpha release boundary: `docs/alpha-release-contract.md`
