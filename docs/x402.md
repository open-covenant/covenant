# Covenant x402

x402 is HTTP `402 Payment Required` turned into a payment rail: a resource
answers an unpaid request with a price requirement, the caller authorizes an
onchain payment, and the request is retried with an `X-PAYMENT` header. Covenant
operates inbound paid resources. The repository also contains reusable outbound
client and signer primitives, but daemon-owned outbound payment is parked.

The deployed inbound seller and reusable outbound signer are separate
integration surfaces. The seller speaks x402 v2. The reusable Solana signer
emits the repository's legacy v1 payment envelope; it does not become
v2-compatible because the seller is. The production daemon refuses direct
`PayX402`, Hyre paid execution, and AceData keyless x402. Those paths lack a
transaction-bound authorization and durable prepayment reservation, so they are
not production W009 enforcement.

## Crates

| Crate | Role |
| --- | --- |
| `covenant-x402` | Core types (`PaymentRequirements`, `PaymentExtra`), the `Signer` trait, and `PayaiSolanaSigner` for Solana settlement. |
| `covenant-x402-signer` | A stdin/stdout sidecar binary: read legacy-v1 `PaymentRequirements` JSON on stdin, get the base64 `X-PAYMENT` header value on stdout. Moving the key into a subprocess keeps it out of the daemon process but is not, by itself, a wallet-security or policy-enforcement boundary. |

## Money path (Solana)

- **Network / asset:** Solana mainnet, USDC (`EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v`).
- **Facilitator:** [PayAI](https://facilitator.payai.network) is the fee payer and the settler. It co-signs the transaction as `feePayer` (so it sponsors gas) and submits it. Its `/supported` advertises x402 v2 `exact` on Solana mainnet.
- **Roles:** the *funder* (the paying agent) partial-signs the transfer; the *facilitator* co-signs as fee payer and settles; the *recipient* (`payTo`) receives the USDC. The funder and the recipient are always distinct accounts.
- **Delivery and settlement are separate:** a `402`, resource error, or timeout does not prove that settlement failed. Do not assume no charge. Inspect the facilitator response and confirm the transaction or recipient USDC balance on chain before retrying.

## Paying for resources (outbound)

The production daemon does not currently make x402 payments. Even with legacy
environment flags set, direct `PayX402`, Hyre paid tools, and AceData's keyless
fallback fail closed before network or signer activity. The previous flow paid
before a durable accounting reservation and accepted caller-controlled policy
fields, so capability labels and post-payment receipts were not an adequate
wallet boundary.

The `covenant-x402-signer` sidecar remains an explicit development primitive.
It accepts legacy-v1 `PaymentRequirements` and returns a payment header; it does
not validate a trusted destination or consume the advisory preflight receipt.
An operator using it manually remains responsible for validating the entire
challenge, preventing duplicate payment, and reconciling settlement before a
retry.

```bash
export COVENANT_X402_FUNDING_KEYPAIR=/path/to/funder.json
export COVENANT_X402_RPC_URL=https://api.mainnet-beta.solana.com
echo "$payment_requirements_json" | covenant-x402-signer   # -> base64 X-PAYMENT header
```

## Exposing a paid resource (inbound)

Covenant runs a public x402-v2 seller at **`https://x402-seller.opencovenant.org`**.
It sells evidence endpoints, all advertised at `GET /.well-known/x402`:

```
GET  /x402/passport/<mpl-core-asset>   registration and configured-record observations
POST /x402/attest                      caller-supplied statement signed under the seller key
GET  /x402/payai/reputation/<wallet>   legacy heuristic over bounded PayAI-linked transfers
GET  /x402/er/enclave/<validator>      the seller's signed DCAP-monitor result for a validator quote
```

A valid signature proves only that the bytes were signed by the accompanying
key. Attributing that key to Covenant requires an expected key pinned through a
trusted external channel; serving the key beside the statement is not
independent attribution. Registration and payment observations do not prove
real-world identity, x402 job delivery, quality, reputation, or W009/W011
enforcement. The enclave endpoint trusts the seller implementation, issuer,
endpoint selection, and collateral handling; optional subject bytes in its
challenge do not prove that an agent record originated inside the enclave.

Unpaid, each returns an x402 v2 `402` challenge: `exact` scheme, Solana mainnet,
USDC, `payTo` the Covenant treasury, `feePayer` the PayAI sponsor, priced per
call (currently `$0.001` to `$0.01`). Pay and retry to receive the signed
result. Resource delivery and settlement are separate: on an error or timeout,
do not assume no charge. Inspect the facilitator response and confirm the
transaction or recipient USDC balance on chain before retrying.

The `/passport` and `/reputation` path segments above are legacy compatibility
names. They do not change the response semantics: the former returns
registration/configured-record observations and the latter returns a bounded
transfer-activity heuristic, not a passport or reputation credential.

The endpoints are discoverable and monitored:

- `GET /.well-known/x402` advertises them so crawlers (the zauth directory,
  x402scan) can list the resources.
- They are registered and health-monitored in the
  [zauth provider hub](./zauth.md) on Solana mainnet.

## See also

- [Hyre x402 Provider](./hyre-integration.md) — a provider profile on the outbound path.
- [Covenant × zauth](./zauth.md) — discovery and health-monitoring for x402 endpoints.
- [Metaplex integration](./metaplex-integration.md) — on-chain agent identity and attestations.
