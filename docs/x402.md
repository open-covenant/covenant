# Covenant x402

x402 is HTTP `402 Payment Required` turned into a working payment rail: a
resource answers an unpaid request with a signed price quote, the caller pays
on-chain, and the same request retried with an `X-PAYMENT` header returns the
resource. Covenant uses x402 in both directions: agents *pay* for external
resources, and Covenant *exposes* paid resources to other agents.

Both directions run through one accounting path. Settlement, budgeting, audit,
and attestation are shared, so a paid call is a first-class, capability-gated,
audited action rather than a side channel.

## Crates

| Crate | Role |
| --- | --- |
| `covenant-x402` | Core types (`PaymentRequirements`, `PaymentExtra`), the `Signer` trait, and `PayaiSolanaSigner` for Solana settlement. |
| `covenant-x402-signer` | A stdin/stdout sidecar binary: read a `PaymentRequirements` JSON on stdin, get the base64 `X-PAYMENT` header value on stdout. Keeps signing out of process. |

## Money path (Solana)

- **Network / asset:** Solana mainnet, USDC (`EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v`).
- **Facilitator:** [PayAI](https://facilitator.payai.network) is the fee payer and the settler. It co-signs the transaction as `feePayer` (so it sponsors gas) and submits it. Its `/supported` advertises x402 v2 `exact` on Solana mainnet.
- **Roles:** the *funder* (the paying agent) partial-signs the transfer; the *facilitator* co-signs as fee payer and settles; the *recipient* (`payTo`) receives the USDC. The funder and the recipient are always distinct accounts.
- **Fail closed:** if the facilitator cannot settle, the caller receives a `402` and is never charged, and the resource is never released.

## Paying for resources (outbound)

An agent reaches a paid provider through capability-gated MCP tools. The daemon
signs the x402 payment with the agent's funding key, debits the agent's budget,
writes a settlement receipt, and records the call in the hash-chained audit log.
The agent never holds raw payment plumbing; it calls a tool and gets a result.

[Hyre](./hyre-integration.md) is the worked example of a provider profile built
on this path. The `covenant-x402-signer` sidecar is the reusable primitive:
feed it the `PaymentRequirements` from any `402` challenge and it returns the
header to retry with.

```bash
export COVENANT_X402_FUNDING_KEYPAIR=/path/to/funder.json
export COVENANT_X402_RPC_URL=https://api.mainnet-beta.solana.com
echo "$payment_requirements_json" | covenant-x402-signer   # -> base64 X-PAYMENT header
```

## Exposing a paid resource (inbound)

Covenant runs a public x402 seller at **`https://x402-seller.opencovenant.org`**.
It sells a Covenant agent reputation/attestation lookup:

```
GET /x402/agent/<solana-pubkey>
```

Unpaid, it returns an x402 v2 `402` challenge: `exact` scheme, Solana mainnet,
USDC, `payTo` the Covenant treasury, `feePayer` the PayAI sponsor, price
`$0.001`. Pay and retry, and it returns a `covenant_agent_attestation_v0`
object for that pubkey. Returning a `>= 400` from the resource cancels
settlement, so a caller is never charged for an error.

The endpoint is discoverable and monitored:

- `GET /.well-known/x402` advertises the resource so crawlers (the zauth
  directory, x402scan) can list it.
- It is registered and health-monitored in the
  [zauth provider hub](./zauth.md) on Solana mainnet.

The v0 attestation surface returns verified on-chain presence today; richer
reputation fields wire to the Covenant audit/reputation layer next.

## See also

- [Hyre x402 Provider](./hyre-integration.md) — a provider profile on the outbound path.
- [Covenant × zauth](./zauth.md) — discovery and health-monitoring for x402 endpoints.
- [Metaplex integration](./metaplex-integration.md) — on-chain agent identity and attestations.
