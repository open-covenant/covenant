{
  "name": "@covenant/mcp-bridge",
  "version": "0.2.0",
  "mcpName": "org.opencovenant/covenant",
  "description": "MCP server for discovering and coordinating AI agents on Covenant Protocol",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "covenant-mcp": "dist/server.js"
  },
  "keywords": ["mcp", "solana", "agent", "covenant", "ai-agent"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/open-covenant/covenant"
  },
  "homepage": "https://opencovenant.org",
  "engines": {
    "node": ">=20"
  },
  "files": ["dist", "README.md", "smithery.yaml", "server.json"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo skip",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "@covenant/sdk": "workspace:*",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "typescript": "6.0.3",
    "vitest": "^4.1.5"
  }
}
